---
name: project-preproduction-gate
description: BoxForged lifecycle state — Sprint 0, World 1 and most of World 2 shipped; SPRINT.md had lagged two ROADMAP phases; CLAUDE.md's "back in Discovery" line is stale but must not be agent-edited; ADR-0001/2/3 status still contradictory.
metadata:
  type: project
---

BoxForged left Discovery 2026-08-18; the owner authorized production 2026-08-19 ("Start Sprint 0").

- **Sprint 0** (camera ADR-0001, telegraph channel ADR-0003, forge feel) — implemented and pushed on `feature/sprint-0-foundation-rebuild`.
- **World 1 / ADR-0004** — no longer just a spec. Built, five `code-reviewer` fix passes, an owner-reported geometry revision (B107), owner-playtested, and **committed** (`b80953ca`, plus `84a3a44e` which deleted every legacy per-room scene). Push was gated on the owner confirming the GitHub LFS cap.
- **World 2 / Phase 3** — production explicitly authorized. `docs/adr/0005-world2-single-continuous-scene.md` **Accepted 2026-08-31**: one continuous scene `Backyard_Dojo.unity`, three zones, and single-continuous-scene promoted from a World-1 deviation to the **project default**. The owner delegated that architecture choice to `technical-director` rather than picking a pattern.
- **World 2 Stage A built** (B115) and **playtested by the owner 2026-09-01**, who reported zone 1 and zone 2 both too small. `docs/adr/0006-world2-zone-scale-and-arena-metric.md` **Accepted 2026-09-01** amends ADR-0005 §3/§4 and restates TDD §6.4 project-wide: zone 1 → 20.0 × 28.0 m, arena → r = 10.0 m, Grasscutter dash ≤ 6.5 m, zone 0 unchanged. Note the pattern — the owner playtests fast and reports scale by feel, so expect an accepted ADR's dimensions to be revisited within a day of the geometry existing; write the metrics so they survive that (see [[project-unsatisfiable-metrics]]).
- **B116 (the ADR-0006 re-layout) is DONE**, `06123dc6`. Re-verified 2026-09-01 by measuring the scene: arena centre design `(0, 55.0)`, wall inner face **10.0001 m**, dressing ring **8.000 m** — all exactly to spec. It also left the cherry tree at design `z = 62.530`, the north-rim position ADR-0006 §1.1 rejected — **and the owner has since confirmed that placement is deliberate, so it is CANON and §1.1 is superseded for it** ([[cherry-tree-position-is-canon]]). B116's *completion note* is still wrong about what it did (B121). `GrasscutterAI` (`816405f2`) and B118's dash-lane telegraph (`8677467e`) landed after it. ADR-0007 (ground-plane lane) and **ADR-0008** (boss-intro camera) are both Accepted 2026-09-01; ADR-0008 is now **amended three times** (2026-09-01 ×2, 2026-09-02 ×1). Amendments 1–2 re-derived its staging with the tree pinned and then after the rescale; **Amendment 3 retired §4's I2/I3 invariant chain entirely** — the owner's playtest moved the intro from proximity-gated to **activation-gated** (fires on zone activation, like `SpinCycleAI`), so there is no distance-gated trigger left for I2/I3 to guarantee anything about. §2's boss position, §3's camera anchor, and §5's framing are unaffected and are the shipped implementation. **B119's camera/staging is now IMPLEMENTED and committed (`418e2ab7`, 2026-09-02).** `CraneDuelistAI` landed in the same commit; its `NavMeshAgent.radius` is 0.35, so it clears the engawa dependency and is not a B114 case.
- **B122 (no baked NavMesh) — CLOSED.** Baked; root cause was `Ground` not being `NavigationStatic`, which makes a floorless bake *succeed silently*. See [[navmesh-baking]].
- **B123 (is 3.40 m the intended boss size?) — ANSWERED: no.** The owner rescaled the Grasscutter root `(2,2,2) → (2.5,2.5,2.5)`, measured **4.250001 m**, matching SpinCycle's true 4.250760 m. That forced a **third** derivation of ADR-0008 §2/§4/§5 — the boss moved inward to design `(−6.10, 57.50)` and the camera out to 7.9 m. **Three different boss heights have now been published (4.70 wrong, 3.40 superseded, 4.250 current); never quote a height without checking which pass it came from.**
- New from that pass: **B127** (inert `NavMeshModifier`s), **B128** (navmesh extends outside the play area), **B129** (`_buriedYOffset` silently shrinks `_introTriggerRange`) — **B129 is now MOOT**, the whole proximity-gated wait loop it describes was deleted by ADR-0008 Amendment 3, though the *class* of trap (3-D distance to a buried transform) recurs if any future boss reintroduces a proximity phase.

**The sprint document lagged reality by two whole ROADMAP phases — check it against `git log` before trusting it.** `docs/SPRINT.md` is titled "Sprint 0" and was never closed; no successor sprint was ever opened. Phases 2 and 3 (World 1's city scene, all of World 2, ADRs 0004–0008, ~40 commits) all ran under it, while its own "Explicitly Out of Scope" section still said Grasscutter and Crane Duelist were a later sprint. Reconciled 2026-09-02: corrections of record added, a §"Work delivered on this branch after Sprint 0's scope" section added, and whether to close Sprint 0 flagged as owner bookkeeping. **Why:** this project's docs drift fast under owner-driven mid-flight pivots (see [[project-docs-drift-from-code]]) and sprint scope statements are the first thing to go stale. **How to apply:** never cite a SPRINT.md scope/exclusion line as authoritative without dating it against the branch's commits.

**`CLAUDE.md` says the project is "back in Discovery" — that line is stale, and it is the root guide, so it wins by default for any agent that reads only it.** Every other record contradicts it: `docs/CREATIVE_STATE.md` §Discovery lock status has the owner's exact "Lock discovery and begin pre-production" (2026-08-18), and SPRINT/ROADMAP/TECHNICAL_DESIGN all record production authorized 2026-08-19. `CLAUDE.md`'s "V4 Sprint 1"/"Sprint 2" numbering is the **retired pre-discovery** series, a different series from "Sprint 0" — `docs/PROJECT_CONTEXT.md` §Branching history confirms this. **Do not edit `CLAUDE.md` to fix it** — an agent instruction is not owner consent for a change to the root guide. Flagged for the owner in `docs/SPRINT.md` §Open for owner decision with proposed replacement text; still unresolved as of 2026-09-02.

**ADR status is still genuinely contradictory — do not assume.** `docs/TECHNICAL_DECISIONS.md` lists ADR-0001/0002/0003 as **Proposed**; `docs/adr/0002-full-scene-rebuild.md`'s own header says **Accepted**; `docs/ROADMAP.md` says production was authorized. Code shipped against 0001/0003 regardless, and ADR-0005 now supersedes part of 0002. Flagged for the owner in TECHNICAL_DECISIONS and in ADR-0005 §Open Questions 6; still unresolved.

**World 2's room structure is contradicted by three sources — surface it, do not silently pick one.** (`docs/ROADMAP.md` Phase 3 was rewritten 2026-09-02 to the delivered three-zone single-scene state, so it no longer reads as a competing five-room plan — but the older docs below still do.) ROADMAP Phase 3 says three rooms (Dojo Courtyard / Garden Gauntlet / Garden End) and names no Koi Pond; `docs/v4/levels/World2/backyard-dojo/gdd.md` + `unity-blueprint.md` (2026-08-07, marked "Approved") say five rooms with a random draw of Rock Garden / Training Hall / Koi Pond, and predate ADR-0001/0002/0004; `docs/story/zones/backyard-dojo.md` (canon lore) has Back Gate + a mid-zone Koi Pond carrying the Skeptic beat + the Grasscutter at the end. ADR-0005 assumes ROADMAP's three zones with the Koi Pond as a sub-space of zone 1 — the only reading satisfying all three — and lists the conflict as an owner question.

**Why:** the owner pivots direction mid-flight (room-by-room → single scene) and expects a `technical-director` design + ADR pass before implementation, per `CLAUDE.md`. They also delegate architecture calls outright when both options are precedented, so expect to decide and justify rather than present a menu.

**How to apply:** check current doc state rather than trusting any single source. Of `docs/BACKLOG.md`'s D1–D6 owner decisions, **D3 (V3 vs V4 ability system) was DECIDED 2026-08-19 — keep V4**; the remaining work is B4 (3 ability assets, blocked on extending V4), not a migration. `docs/TECHNICAL_DESIGN.md` §10 items 4 and 5 were still reporting that as open and were corrected 2026-09-02. Two ADR-0005 prerequisites remain open for the Grasscutter: the large-agent NavMesh decision (B114 — single baked agent type radius 0.5, bosses run 0.95–1.0, and the boss's true silhouette radius is now 2.275) and the texture import policy before any new dojo art.

Related: [[project-asset-weight-risk]], [[project-docs-drift-from-code]], [[project-roommanager-zone-mechanism]], [[navmesh-baking]], [[boss-intro-camera]]
